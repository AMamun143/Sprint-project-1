[project]
name = "sprint-project-1"
version = "0.1.0"
description = "Data cleaning and visualization sprint project"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "matplotlib",
    "seaborn",
]
